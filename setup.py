from setuptools import setup, find_packages

setup(name='spectralminealanalyzer',
      version='0.0.1',
      packages=find_packages(),
      author='Pedro Silva',
      python_requires='>=3.8'
      )